diff --git a/wmt-launcher/firmware.c b/wmt-launcher/firmware.c
index c3bf3dc..d40e002 100644
--- a/wmt-launcher/firmware.c
+++ b/wmt-launcher/firmware.c
@@ -108,7 +108,7 @@ static int read_patch(int fd, const struct stat *status, const char *name,
 int wmt_firmware_search(const char *directory, uint16_t firmware,
                         struct wmt_firmware_result *output)
 {
-    struct wmt_firmware_result found = {0};
+    struct wmt_firmware_result found = {.patches = {0}};
     int error;
 
     if (!directory || !directory[0] || !output)
diff --git a/wmt-launcher/main.c b/wmt-launcher/main.c
index ebc9d51..09b36ac 100644
--- a/wmt-launcher/main.c
+++ b/wmt-launcher/main.c
@@ -255,8 +255,8 @@ static int send_frame(int fd, const uint8_t *frame, size_t size)
 static int handle_command(int fd, const char *directory, const struct wmt_command *command)
 {
     uint8_t frame[WMT_CMD2_WRITE_MAX];
-    struct wmt_cmd2_record records[WMT_CMD2_PATCH_MAX] = {0};
-    struct wmt_firmware_result firmware = {0};
+    struct wmt_cmd2_record records[WMT_CMD2_PATCH_MAX] = {{0}};
+    struct wmt_firmware_result firmware = {.patches = {0}};
     struct wmt_rom_set rom = {0};
     bool normal = !strcmp(command->text, "srh_patch");
     bool is_rom = !strcmp(command->text, "srh_rom_patch");
@@ -383,7 +383,7 @@ int main(int argc, char **argv)
     struct sigaction action = {.sa_handler = request_stop};
     struct wmt_cmd2_session session = {.version = WMT_CMD2_VERSION, .action = WMT_CMD2_BIND};
     struct power_task power = {.argument = 1};
-    struct optional_controls controls = {0};
+    struct optional_controls controls = {.fwlog = {0}};
     pthread_t power_thread;
     bool bound = false, power_started = false, advertised = false;
     int option, result = -EINTR, fd = -1;
