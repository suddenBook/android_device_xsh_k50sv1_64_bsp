From b73c8f13ecbbf27ea0d5e87f5afd214ee5eb1c8f Mon Sep 17 00:00:00 2001
From: Huixiaqing Liu <huixiaqingliu@outlook.com>
Date: Mon, 7 Sep 2026 08:19:18 +0200
Subject: [PATCH 4/8] media: restore IMX145 BU6424AF selection and propagate
 lens I2C failures

---
 .../configs/k50sv1_64_bsp_debug_defconfig     |   2 +-
 arch/arm64/configs/k50sv1_64_bsp_defconfig    |   2 +-
 .../configs/k50sv1_64_bsp_source.fragment     |   5 +-
 .../configs/k50sv1_64_bsp_wusb_off.fragment   |   4 +-
 drivers/misc/mediatek/lens/main/Makefile      |   3 +
 .../lens/main/common/bu6424af/BU6424AF.c      |  50 +++----
 .../lens/main/common/dw9761af/DW9761AF.c      |   8 +-
 drivers/misc/mediatek/lens/main/main_lens.c   |   6 +-
 tools/testing/bu6424af/run.py                 | 124 ++++++++++++++++++
 tools/testing/dw9761af/run.sh                 |   8 +-
 10 files changed, 164 insertions(+), 48 deletions(-)
 create mode 100644 tools/testing/bu6424af/run.py

diff --git a/arch/arm64/configs/k50sv1_64_bsp_debug_defconfig b/arch/arm64/configs/k50sv1_64_bsp_debug_defconfig
index 24d28807055..b91d6a525d3 100644
--- a/arch/arm64/configs/k50sv1_64_bsp_debug_defconfig
+++ b/arch/arm64/configs/k50sv1_64_bsp_debug_defconfig
@@ -251,7 +251,7 @@ CONFIG_MTK_LENS_AD5820AF_SUPPORT=y
 CONFIG_MTK_LENS_BU6424AF_SUPPORT=y
 CONFIG_MTK_LENS_BU6429AF_SUPPORT=y
 CONFIG_MTK_LENS_DW9714AF_SUPPORT=y
-CONFIG_MTK_LENS_DW9761AF_SUPPORT=y
+# CONFIG_MTK_LENS_DW9761AF_SUPPORT is not set
 CONFIG_MTK_LENS_LC898212AF_SUPPORT=y
 CONFIG_MTK_LENS_LC898214AF_SUPPORT=y
 CONFIG_MTK_MMPROFILE_SUPPORT=y
diff --git a/arch/arm64/configs/k50sv1_64_bsp_defconfig b/arch/arm64/configs/k50sv1_64_bsp_defconfig
index 3a552ef442e..39f6a6cad24 100644
--- a/arch/arm64/configs/k50sv1_64_bsp_defconfig
+++ b/arch/arm64/configs/k50sv1_64_bsp_defconfig
@@ -249,7 +249,7 @@ CONFIG_MTK_LENS_AD5820AF_SUPPORT=y
 CONFIG_MTK_LENS_BU6424AF_SUPPORT=y
 CONFIG_MTK_LENS_BU6429AF_SUPPORT=y
 CONFIG_MTK_LENS_DW9714AF_SUPPORT=y
-CONFIG_MTK_LENS_DW9761AF_SUPPORT=y
+# CONFIG_MTK_LENS_DW9761AF_SUPPORT is not set
 CONFIG_MTK_LENS_LC898212AF_SUPPORT=y
 CONFIG_MTK_LENS_LC898214AF_SUPPORT=y
 CONFIG_MTK_MMPROFILE_SUPPORT=y
diff --git a/arch/arm64/configs/k50sv1_64_bsp_source.fragment b/arch/arm64/configs/k50sv1_64_bsp_source.fragment
index 4c94e84cb94..09058670020 100644
--- a/arch/arm64/configs/k50sv1_64_bsp_source.fragment
+++ b/arch/arm64/configs/k50sv1_64_bsp_source.fragment
@@ -31,10 +31,11 @@ CONFIG_MTK_CAM_CAL=y
 CONFIG_CUSTOM_KERNEL_CAM_CAL_DRV="s5k2p8_eeprom ov8858_eeprom"
 
 CONFIG_MTK_LENS=y
-CONFIG_MTK_LENS_DW9761AF_SUPPORT=y
+# The IMX145 HAL's sensor-matched lens table selects BU6424AF at I2C 0x0c.
+# CONFIG_MTK_LENS_DW9761AF_SUPPORT is not set
 # CONFIG_MTK_LENS_DUMMYLENS_SUPPORT is not set
 # CONFIG_MTK_LENS_AD5820AF_SUPPORT is not set
-# CONFIG_MTK_LENS_BU6424AF_SUPPORT is not set
+CONFIG_MTK_LENS_BU6424AF_SUPPORT=y
 # CONFIG_MTK_LENS_BU6429AF_SUPPORT is not set
 # CONFIG_MTK_LENS_DW9714AF_SUPPORT is not set
 # CONFIG_MTK_LENS_LC898212AF_SUPPORT is not set
diff --git a/arch/arm64/configs/k50sv1_64_bsp_wusb_off.fragment b/arch/arm64/configs/k50sv1_64_bsp_wusb_off.fragment
index c9422e0d200..2e56757ca63 100644
--- a/arch/arm64/configs/k50sv1_64_bsp_wusb_off.fragment
+++ b/arch/arm64/configs/k50sv1_64_bsp_wusb_off.fragment
@@ -29,10 +29,10 @@ CONFIG_MTK_CAM_CAL=y
 CONFIG_CUSTOM_KERNEL_CAM_CAL_DRV="s5k2p8_eeprom ov8858_eeprom"
 
 CONFIG_MTK_LENS=y
-CONFIG_MTK_LENS_DW9761AF_SUPPORT=y
+# CONFIG_MTK_LENS_DW9761AF_SUPPORT is not set
 # CONFIG_MTK_LENS_DUMMYLENS_SUPPORT is not set
 # CONFIG_MTK_LENS_AD5820AF_SUPPORT is not set
-# CONFIG_MTK_LENS_BU6424AF_SUPPORT is not set
+CONFIG_MTK_LENS_BU6424AF_SUPPORT=y
 # CONFIG_MTK_LENS_BU6429AF_SUPPORT is not set
 # CONFIG_MTK_LENS_DW9714AF_SUPPORT is not set
 # CONFIG_MTK_LENS_LC898212AF_SUPPORT is not set
diff --git a/drivers/misc/mediatek/lens/main/Makefile b/drivers/misc/mediatek/lens/main/Makefile
index 4e457af094c..4a67fe3db6d 100644
--- a/drivers/misc/mediatek/lens/main/Makefile
+++ b/drivers/misc/mediatek/lens/main/Makefile
@@ -25,6 +25,9 @@ subdir-ccflags-y += -I$(MTK_CUSTOM_PATH)/lens/main/inc
 obj-$(CONFIG_MTK_LENS)    += mainaf.o
 
 mainaf-y  += main_lens.o
+ifeq ($(CONFIG_MTK_LENS_BU6424AF_SUPPORT), y)
+mainaf-y  += common/bu6424af/BU6424AF.o
+endif
 ifeq ($(CONFIG_MTK_LENS_DW9761AF_SUPPORT), y)
 mainaf-y  += common/dw9761af/DW9761AF.o
 endif
diff --git a/drivers/misc/mediatek/lens/main/common/bu6424af/BU6424AF.c b/drivers/misc/mediatek/lens/main/common/bu6424af/BU6424AF.c
index 928ba99810f..d323f3f89ff 100644
--- a/drivers/misc/mediatek/lens/main/common/bu6424af/BU6424AF.c
+++ b/drivers/misc/mediatek/lens/main/common/bu6424af/BU6424AF.c
@@ -39,7 +39,6 @@
 static struct i2c_client *g_pstAF_I2Cclient;
 static int *g_pAF_Opened;
 static spinlock_t *g_pAF_SpinLock;
-static int g_i4DriverStatus;
 
 static unsigned long g_u4AF_INF;
 static unsigned long g_u4AF_MACRO = 1023;
@@ -58,10 +57,9 @@ static int s4AF_ReadReg(unsigned short *a_pu2Result)
 
 	i4RetValue = i2c_master_recv(g_pstAF_I2Cclient, pBuff, 2);
 
-	if (i4RetValue < 0) {
-		g_i4DriverStatus++;
-		LOG_INF("I2C read failed - %d!!\n", g_i4DriverStatus);
-		return -1;
+	if (i4RetValue != 2) {
+		LOG_INF("I2C read failed: %d\n", i4RetValue);
+		return i4RetValue < 0 ? i4RetValue : -EIO;
 	}
 
 	*a_pu2Result = (((u16)(pBuff[0] & 0x03)) << 8) + pBuff[1];
@@ -81,10 +79,9 @@ static int s4AF_WriteReg(u16 a_u2Data)
 
 	i4RetValue = i2c_master_send(g_pstAF_I2Cclient, puSendCmd, 2);
 
-	if (i4RetValue < 0) {
-		g_i4DriverStatus++;
-		LOG_INF("I2C read failed - %d!!\n", g_i4DriverStatus);
-		return -1;
+	if (i4RetValue != 2) {
+		LOG_INF("I2C write failed: %d\n", i4RetValue);
+		return i4RetValue < 0 ? i4RetValue : -EIO;
 	}
 
 	return 0;
@@ -92,7 +89,7 @@ static int s4AF_WriteReg(u16 a_u2Data)
 
 static inline int getAFInfo(__user struct stAF_MotorInfo *pstMotorInfo)
 {
-	struct stAF_MotorInfo stMotorInfo;
+	struct stAF_MotorInfo stMotorInfo = {0};
 
 	stMotorInfo.u4MacroPosition = g_u4AF_MACRO;
 	stMotorInfo.u4InfPosition = g_u4AF_INF;
@@ -107,7 +104,7 @@ static inline int getAFInfo(__user struct stAF_MotorInfo *pstMotorInfo)
 		stMotorInfo.bIsMotorOpen = 0;
 
 	if (copy_to_user(pstMotorInfo, &stMotorInfo, sizeof(struct stAF_MotorInfo)))
-		LOG_INF("copy to user failed when getting motor information\n");
+		return -EFAULT;
 
 	return 0;
 }
@@ -116,9 +113,6 @@ static inline int moveAF(unsigned long a_u4Position)
 {
 	int ret = 0;
 
-	if (g_i4DriverStatus > 2) /* I2C failed */
-		return -EINVAL;
-
 	if ((a_u4Position > g_u4AF_MACRO) || (a_u4Position < g_u4AF_INF)) {
 		LOG_INF("out of range\n");
 		return -EINVAL;
@@ -128,21 +122,11 @@ static inline int moveAF(unsigned long a_u4Position)
 		unsigned short InitPos;
 
 		ret = s4AF_ReadReg(&InitPos);
-
-		if (ret == 0) {
-			LOG_INF("Init Pos %6d\n", InitPos);
-
-			spin_lock(g_pAF_SpinLock);
-			g_u4CurrPosition = (unsigned long)InitPos;
-			spin_unlock(g_pAF_SpinLock);
-
-		} else {
-			spin_lock(g_pAF_SpinLock);
-			g_u4CurrPosition = 0;
-			spin_unlock(g_pAF_SpinLock);
-		}
+		if (ret)
+			return ret;
 
 		spin_lock(g_pAF_SpinLock);
+		g_u4CurrPosition = InitPos;
 		*g_pAF_Opened = 2;
 		spin_unlock(g_pAF_SpinLock);
 	}
@@ -157,13 +141,13 @@ static inline int moveAF(unsigned long a_u4Position)
 	/* LOG_INF("move [curr] %d [target] %d\n", g_u4CurrPosition, g_u4TargetPosition); */
 
 
-	if (s4AF_WriteReg((unsigned short)g_u4TargetPosition) == 0) {
+	ret = s4AF_WriteReg((unsigned short)g_u4TargetPosition);
+	if (!ret) {
 		spin_lock(g_pAF_SpinLock);
 		g_u4CurrPosition = (unsigned long)g_u4TargetPosition;
 		spin_unlock(g_pAF_SpinLock);
 	} else {
 		LOG_INF("set I2C failed when moving the motor\n");
-		ret = -1;
 	}
 
 	return ret;
@@ -171,6 +155,8 @@ static inline int moveAF(unsigned long a_u4Position)
 
 static inline int setAFInf(unsigned long a_u4Position)
 {
+	if (a_u4Position > g_u4AF_MACRO)
+		return -EINVAL;
 	spin_lock(g_pAF_SpinLock);
 	g_u4AF_INF = a_u4Position;
 	spin_unlock(g_pAF_SpinLock);
@@ -179,6 +165,8 @@ static inline int setAFInf(unsigned long a_u4Position)
 
 static inline int setAFMacro(unsigned long a_u4Position)
 {
+	if (a_u4Position > 1023 || a_u4Position < g_u4AF_INF)
+		return -EINVAL;
 	spin_lock(g_pAF_SpinLock);
 	g_u4AF_MACRO = a_u4Position;
 	spin_unlock(g_pAF_SpinLock);
@@ -209,7 +197,7 @@ long BU6424AF_Ioctl(struct file *a_pstFile, unsigned int a_u4Command, unsigned l
 
 	default:
 		LOG_INF("No CMD\n");
-		i4RetValue = -EPERM;
+		i4RetValue = -ENOTTY;
 		break;
 	}
 
@@ -242,8 +230,6 @@ int BU6424AF_Release(struct inode *a_pstInode, struct file *a_pstFile)
 		spin_unlock(g_pAF_SpinLock);
 	}
 
-	g_i4DriverStatus = 0;
-
 	LOG_INF("End\n");
 
 	return 0;
diff --git a/drivers/misc/mediatek/lens/main/common/dw9761af/DW9761AF.c b/drivers/misc/mediatek/lens/main/common/dw9761af/DW9761AF.c
index 85abeb3cdf5..1b8f8886b16 100644
--- a/drivers/misc/mediatek/lens/main/common/dw9761af/DW9761AF.c
+++ b/drivers/misc/mediatek/lens/main/common/dw9761af/DW9761AF.c
@@ -1,11 +1,9 @@
 /*
  * Copyright (C) 2026 The LineageOS Project
  *
- * Fitted Dongwoon DW9761 VCM on k50sv1 MAINAF.  Stock Image names
- * drivers/misc/mediatek/lens/main/common/dw9761af/DW9761AF.c, logs
- * DW9761AF_DRV, and aliases Ioctl/Release as *_Main.  I2C write-id 0x18
- * matches live camera_main_af@0c.  Position uses DW9761 SAC registers
- * 0x03/0x04, not DW9714's packed 10-bit pair.
+ * Dongwoon DW9761 VCM variant. The k50sv1 IMX145 HAL selects BU6424AF;
+ * this register-addressed implementation is not interchangeable with it
+ * and is disabled in the board configuration.
  *
  * This program is free software; you can redistribute it and/or modify
  * it under the terms of version 2 of the GNU General Public License as
diff --git a/drivers/misc/mediatek/lens/main/main_lens.c b/drivers/misc/mediatek/lens/main/main_lens.c
index 87ff6e02b69..92cf5269d99 100644
--- a/drivers/misc/mediatek/lens/main/main_lens.c
+++ b/drivers/misc/mediatek/lens/main/main_lens.c
@@ -72,6 +72,9 @@ static struct i2c_board_info kd_lens_dev __initdata = {
 #define LOG_INF(format, args...)
 #endif
 static struct stAF_DrvList g_stAF_DrvList[MAX_NUM_OF_LENS] = {
+#ifdef CONFIG_MTK_LENS_BU6424AF_SUPPORT
+	{1, AFDRV_BU6424AF, BU6424AF_SetI2Cclient, BU6424AF_Ioctl, BU6424AF_Release},
+#endif
 #ifdef CONFIG_MTK_LENS_DW9761AF_SUPPORT
 	{1, AFDRV_DW9761AF, DW9761AF_SetI2Cclient, DW9761AF_Ioctl, DW9761AF_Release},
 #endif
@@ -113,6 +116,8 @@ static long AF_SetMotorName(__user struct stAF_MotorName *pstMotorName)
 	if (copy_from_user(&stMotorName, pstMotorName,
 			   sizeof(struct stAF_MotorName)))
 		return -EFAULT;
+	if (!memchr(stMotorName.uMotorName, '\0', sizeof(stMotorName.uMotorName)))
+		return -EINVAL;
 
 	LOG_INF("Set Motor Name : %s\n", stMotorName.uMotorName);
 
@@ -121,7 +126,6 @@ static long AF_SetMotorName(__user struct stAF_MotorName *pstMotorName)
 			break;
 
 		LOG_INF("Search Motor Name : %s\n", g_stAF_DrvList[i].uDrvName);
-		pr_err("songbl Search Motor Name : %s  g_stAF_DrvList[%d].uDrvName:%s\n",stMotorName.uMotorName,i, g_stAF_DrvList[i].uDrvName);
 		if (strcmp(stMotorName.uMotorName, g_stAF_DrvList[i].uDrvName) == 0) {
 			g_pstAF_CurDrv = &g_stAF_DrvList[i];
 			i4RetValue = g_pstAF_CurDrv->pAF_SetI2Cclient(g_pstAF_I2Cclient,
diff --git a/tools/testing/bu6424af/run.py b/tools/testing/bu6424af/run.py
new file mode 100644
index 00000000000..737ef3e6551
--- /dev/null
+++ b/tools/testing/bu6424af/run.py
@@ -0,0 +1,124 @@
+#!/usr/bin/env python3
+"""Exercise the actual MAINAF selector and BU6424AF driver with fake I2C."""
+import os
+from pathlib import Path
+import re
+import subprocess
+import tempfile
+
+root = Path(__file__).resolve().parents[3]
+lens = root / "drivers/misc/mediatek/lens/main"
+driver = (lens / "common/bu6424af/BU6424AF.c").read_text()
+driver = re.sub(r'^#include .*$', '', driver, flags=re.M)
+main = (lens / "main_lens.c").read_text()
+table = main[main.index("static struct stAF_DrvList g_stAF_DrvList"):]
+table = table[:table.index("};") + 2]
+selector = main[main.index("static long AF_SetMotorName("):]
+selector = selector[:selector.index("\n}") + 2]
+config = (root / "arch/arm64/configs/k50sv1_64_bsp_source.fragment").read_text()
+defines = "\n".join("#define " + m + " 1" for m in
+                    re.findall(r'^(CONFIG_MTK_LENS_\w+)=y$', config, re.M))
+prefix = r'''
+#include <stdbool.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <string.h>
+#include <errno.h>
+typedef uint8_t u8;
+typedef uint16_t u16;
+typedef uint32_t u32;
+typedef int spinlock_t;
+struct file { int unused; };
+struct inode { int unused; };
+struct i2c_client { unsigned addr; };
+#define __user
+#define pr_debug(...) ((void)0)
+#define pr_info(...) ((void)0)
+#define pr_err(...) ((void)0)
+#define spin_lock(p) ((void)(p))
+#define spin_unlock(p) ((void)(p))
+#include "lens_info.h"
+static int send_result = 2, recv_result = 2, writes, copy_error;
+static unsigned char sent[2];
+static int i2c_master_send(struct i2c_client *c, const char *data, int size) {
+    (void)c;
+    if (size != 2) return -EINVAL;
+    memcpy(sent, data, 2); writes++;
+    return send_result;
+}
+static int i2c_master_recv(struct i2c_client *c, char *data, int size) {
+    (void)c;
+    if (size != 2) return -EINVAL;
+    if (recv_result > 0) data[0] = 0x02;
+    if (recv_result > 1) data[1] = 0x34;
+    return recv_result;
+}
+static int copy_to_user(void *to, const void *from, size_t n) {
+    if (copy_error) return 1;
+    memcpy(to, from, n); return 0;
+}
+#define copy_from_user copy_to_user
+'''
+selection_prefix = r'''
+#define g_pstAF_I2Cclient selector_client
+static struct i2c_client chip;
+static struct i2c_client *selector_client = &chip;
+static struct stAF_DrvList *g_pstAF_CurDrv;
+static spinlock_t g_AF_SpinLock;
+static int g_s4AF_Opened = 1;
+static int DW9761AF_SetI2Cclient(struct i2c_client *c, spinlock_t *l, int *o) {
+    (void)c; (void)l; (void)o; return -ENODEV;
+}
+static long DW9761AF_Ioctl(struct file *f, unsigned int c, unsigned long p) {
+    (void)f; (void)c; (void)p; return -ENOTTY;
+}
+static int DW9761AF_Release(struct inode *i, struct file *f) {
+    (void)i; (void)f; return 0;
+}
+'''
+tests = r'''
+static int failures, checks;
+#define CHECK(name, expr) do { checks++; if (!(expr)) { \
+    failures++; printf("FAIL %s\n", name); } } while (0)
+int main(void) {
+    struct stAF_MotorName name = { "BU6424AF" };
+    struct stAF_MotorInfo info;
+    int before;
+    CHECK("HAL IMX145 lens selection", AF_SetMotorName(&name) == 1);
+    BU6424AF_SetI2Cclient(&chip, &g_AF_SpinLock, &g_s4AF_Opened);
+    CHECK("position command", BU6424AF_Ioctl(NULL, AFIOC_T_MOVETO, 0x2ab) == 0);
+    CHECK("BU6424 wire protocol", chip.addr == 0x0c && sent[0] == 0xc2 && sent[1] == 0xab);
+    CHECK("motor state", getAFInfo(&info) == 0 && info.u4CurrentPosition == 0x2ab);
+    send_result = 1;
+    CHECK("short write fails", moveAF(400) == -EIO);
+    CHECK("failed write preserves position", getAFInfo(&info) == 0 && info.u4CurrentPosition == 0x2ab);
+    send_result = -ENXIO;
+    for (int i = 0; i < 4; i++) CHECK("bus errno preserved", moveAF(400) == -ENXIO);
+    send_result = 2;
+    CHECK("transient failure recovers", moveAF(400) == 0);
+    g_s4AF_Opened = 1; recv_result = 1; before = writes;
+    CHECK("short initial read fails", moveAF(200) == -EIO);
+    CHECK("failed read preserves unopened state", g_s4AF_Opened == 1 && writes == before);
+    recv_result = -ENXIO;
+    CHECK("read errno preserved", moveAF(200) == -ENXIO);
+    recv_result = 2;
+    CHECK("read retry recovers", moveAF(200) == 0);
+    copy_error = 1;
+    CHECK("bad user info pointer fails", getAFInfo(&info) == -EFAULT);
+    copy_error = 0;
+    memset(&name, 'X', sizeof(name));
+    CHECK("unterminated lens name rejected", AF_SetMotorName(&name) == -EINVAL);
+    CHECK("invalid lens range rejected", setAFMacro(1024) == -EINVAL && setAFInf(1024) == -EINVAL);
+    CHECK("shutdown", BU6424AF_Release(NULL, NULL) == 0 && sent[0] == 0 && sent[1] == 0);
+    printf("BU6424AF: %d/%d passed\n", checks - failures, checks);
+    return failures != 0;
+}
+'''
+with tempfile.TemporaryDirectory(prefix="bu6424af-") as tmp:
+    source = Path(tmp) / "test.c"
+    source.write_text(prefix + defines + "\n" + driver + selection_prefix + table + selector + tests)
+    exe = Path(tmp) / "test"
+    subprocess.run([os.environ.get("CC", "cc"), "-std=gnu11", "-g", "-O1",
+                    "-fsanitize=address,undefined", "-fno-omit-frame-pointer",
+                    "-I", str(lens / "inc"), str(source), "-o", str(exe)], check=True)
+    raise SystemExit(subprocess.run([str(exe)]).returncode)
diff --git a/tools/testing/dw9761af/run.sh b/tools/testing/dw9761af/run.sh
index 991dc3b63c5..d020dbcdfa1 100755
--- a/tools/testing/dw9761af/run.sh
+++ b/tools/testing/dw9761af/run.sh
@@ -38,8 +38,8 @@ grep -Fq 'AFDRV_DW9761AF' "${INFO}"
 grep -Fq 'AFDRV_DW9761AF' "${MAIN}"
 grep -Fq 'common/dw9761af/DW9761AF.o' "${MAKEFILE}"
 grep -Fq 'config MTK_LENS_DW9761AF_SUPPORT' "${KCONFIG}"
-grep -Fq 'CONFIG_MTK_LENS_DW9761AF_SUPPORT=y' "${DEFCONFIG}"
-grep -Fq 'CONFIG_MTK_LENS_DW9761AF_SUPPORT=y' "${DEBUG_DEFCONFIG}"
+grep -Fq '# CONFIG_MTK_LENS_DW9761AF_SUPPORT is not set' "${DEFCONFIG}"
+grep -Fq '# CONFIG_MTK_LENS_DW9761AF_SUPPORT is not set' "${DEBUG_DEFCONFIG}"
 grep -Fq 'ret == 2 ? 0 : (ret < 0 ? ret : -EIO)' "${DRIVER}"
 grep -Fq 'ret != 3' "${DRIVER}"
 grep -Fq 'return -EFAULT' "${DRIVER}"
@@ -48,8 +48,8 @@ if grep -Eq 'main2/|sub/' "${TOP_MAKEFILE}"; then
 	exit 1
 fi
 if grep -E '^mainaf-y.*common/' "${MAKEFILE}" | \
-	grep -Fv 'common/dw9761af/' | grep -q .; then
-	echo 'an unfitted MAINAF implementation remains linked' >&2
+	grep -Ev 'common/(dw9761af|bu6424af)/' | grep -q .; then
+	echo 'an unexpected MAINAF implementation remains linked' >&2
 	exit 1
 fi
 if grep -Fq 'proc_create("driver/MAINAF"' "${MAIN}"; then
-- 
2.55.0

