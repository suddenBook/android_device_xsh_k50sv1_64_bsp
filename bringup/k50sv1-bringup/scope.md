# Case Scope

## meta
- case_id: k50sv1-bringup
- created: 2026-08-22T20:12:22+02:00
- operator: local
- project_root: /home/desmond/Downloads/k50sv1_64_bsp
- primary_skill: firmware-pentest/SKILL.md
- primary_id: R8
- lead_role: lead
- specialist_roles: [cre, cae, doc]

## auth
- status: granted
- basis: own_system
- evidence_of_auth: owner explicitly authorized image analysis, rooted-device debugging, flashing the four named partition images without asking each time, internet research, local builds and source-module replacement. On 2026-09-05 at 19:57 UTC the owner delegated incremental/clean builds, userdata/metadata/cache erases and SetupWizard bypass to operator judgment, superseding the earlier mandatory-wipe instruction.

## in_scope
- assets:
  - /home/desmond/Downloads/k50sv1_64_bsp/factory_image
  - /home/desmond/Downloads/k50sv1_64_bsp/factory_image_unpacked
  - /home/desmond/Downloads/k50sv1_64_bsp/lineage-17.1
  - the owner-operated Android handset, over local ADB/fastboot/preloader USB
    and over ADB on the owner's own LAN (192.168.1.251:5555). The LAN transport
    is not a widening of scope: the handset has ONE USB-C port, so any test that
    occupies it -- the wired headset above all -- makes USB debugging physically
    impossible, and network ADB is the only way to observe the device in that
    state. Same handset, same owner, same LAN.
- surfaces: [partition_images, boot_chain, kernel, android_userspace, device_tree, vendor_blobs, local_usb_debug, local_lan_debug]
- activities: [offline_reverse, read_only_device_validation, documentation_correction, internet_research, lineage_build, four_partition_flash, boot_debug]

## out_of_scope
- assets: [unrelated hosts, unrelated USB devices, upstream LineageOS/AOSP repositories]
- activities: [dos, phishing_real_users, unrestricted_exfil, modifying_source_images, upstream_source_changes_without_user_approval]

## network_profile
- mode: authorized_target_only
- notes: |
    Active device traffic is limited to the listed handset, over local USB and
    over the owner's own LAN where the USB port is physically occupied by the
    accessory under test.
    Public primary sources may be read for implementation research.

## deliverables
- report: true
- field_journal: true
- diagrams: false
- timeline: true

## constraints
- timebox: none
- stealth: low
- data_handling: no_user_pii
- workspace_writes: /home/desmond/Downloads/k50sv1_64_bsp/work and device/vendor/kernel subrepositories created for this target
- source_priority: AOSP/Google guidance, then proven same-platform implementations, then stock behavior
- git_policy: commit every logical change; no remote push
- flash_policy: only boot, recovery, system, vendor and logo may be flashed. Choose
  userdata, metadata and cache erases according to the experiment and record each
  actual operation. The owner explicitly superseded the mandatory-wipe rule on
  2026-09-05 at 19:57 UTC; /data holds nothing of value on this handset. Every other
  partition (preloader, lk, modem, nvram, nvdata, protect1/2,
  proinfo, seccfg) stays untouched: those carry IMEI, RF calibration and the boot
  chain, and there is no factory server to restore them from.
  logo was added on 2026-08-24 by owner decision. It qualifies on both counts the
  exclusion list is really about: it holds nothing but boot-splash and charging
  bitmaps -- no identity, no calibration, no boot chain -- and the factory image is
  backed up byte-for-byte at factory_image/logo.bin, so the change is reversible with
  one fastboot command. The replacement is generated by tools/logo/, which rewrites
  only the two byte-identical boot-splash blocks (0 and 38) and carries the
  other 78 through unchanged.

## signoff
- ready_for_act: true
- checklist:
  - [x] auth.status = granted
  - [x] in_scope.assets non-empty
  - [x] network_profile.mode chosen
  - [x] out_of_scope reviewed
  - [x] roles assigned
